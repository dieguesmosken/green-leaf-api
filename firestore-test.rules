rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regras temporárias para teste - PERMISSIVO
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
